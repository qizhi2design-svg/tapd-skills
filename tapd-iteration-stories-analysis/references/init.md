# TAPD 初始化说明

## 触发条件

当 `tapd info` 显示已授权，但当前空间仍是“未设置”时，进入这个文档。

## 未初始化时的处理

如果当前项目还没有默认空间配置，先执行：

```bash
tapd init
```

## 说明

- 迭代下需求分析通常至少需要可用的默认空间或显式 `workspace_id`
- 初始化后优先重新执行 `tapd info`，确认当前空间已设置
